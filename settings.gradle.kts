rootProject.name = "laplacian-arch.model.deployment"

pluginManagement {
    repositories {
        maven(url = "../mvn-repo/")
        maven(url = "https://raw.github.com/nabla-squared/mvn-repo/master/")
        gradlePluginPortal()
        jcenter()
    }
}

// @subprojects@
// Subproject definitions are inserted here.
// @subprojects@