entities:
- name: datastore_component
  subtype_of: container

  properties: []

  relationships:
  - name: spec
    reference_entity_name: datasource
    cardinality: "1"
    mappings:
    - from: name
      to: name