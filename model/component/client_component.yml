entities:
- name: client_component
  subtype_of: container

  properties: []

  relationships:
  - name: spec
    reference_entity_name: client
    cardinality: "1"
    mappings:
    - from: name
      to: name

