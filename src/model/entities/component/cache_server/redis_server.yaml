entities:
- name: redis_server
  namespace: laplacian.arch.deployment
  subtype_of: component

  properties:
  - name: container_image
    type: string
    optional: true
    default_value: |
      "redis"

  - name: port
    type: number
    optional: true
    default_value: |
      6379
